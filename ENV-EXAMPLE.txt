# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Service role key: server-only, NEVER expose to the browser. Used for
# booking creation, slot locking, and verification code checks.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Email (Resend) used for OTP verification, confirmations, reminders ---
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM="Sweet Mee Nails <appointments@sweetmeenails.com>"

# --- App ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Salon's local timezone (IANA), used for all display + reminder scheduling
SALON_TIMEZONE=America/Toronto

# --- Cron / reminders (called by a scheduled GitHub Action or Supabase cron) ---
CRON_SECRET=generate-a-long-random-string
