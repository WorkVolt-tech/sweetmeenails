// Fill these in with your Supabase project's values (Project Settings
// -> API in the Supabase dashboard). The anon key is meant to be public
// — it's what every visitor's browser uses, and Row Level Security /
// the RPC functions in supabase/migrations are what actually keep data
// safe. Never put your service_role key anywhere in this folder.
const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
const SUPABASE_ANON_KEY = "your-anon-key";
